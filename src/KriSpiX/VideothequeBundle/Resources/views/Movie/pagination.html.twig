<ul class="pagination">
    {% for p in range(1, nbPages) %}
    <li{% if p == page %} class="active"{% endif %}>
        <a href="{{ path('krispix_videotheque_home', {'page': p}) }}">{{ p }}</a>
    </li>
    {% endfor %}
</ul>