{% extends "KriSpiXVideothequeBundle::layout.html.twig" %}

{% block title %}
    {{ genre.name }} - {{ parent() }}
{% endblock %}

{% block krispixvideotheque_body %}

    <h1>{{ genre.name }} {{ include("KriSpiXVideothequeBundle:Movie:viewMode.html.twig") }}</h1>
    <hr/>
     <div id="movies" class="row list-group">
        {% for movie in listMovies %}    
            {{ include("KriSpiXVideothequeBundle:Movie:thumb.html.twig") }}
        {% else %}
            <p>Pas (encore !) de films {{ genre.name }}</p>
        {% endfor %}
    </div>

{% endblock %}